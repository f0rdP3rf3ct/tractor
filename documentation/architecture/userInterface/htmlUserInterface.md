# HTML User Interface

This document how the UI elements of the game are solved using HTML, JS and CSS on top of the game instead of using an UI that runs inside the canvas.
This is used this way in order to use all advantages of modern web technology and the components already provided with it.


